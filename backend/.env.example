# AWS SES Configuration
AWS_REGION=us-east-1
SES_FROM_EMAIL=noreply@yourdomain.com
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=movie_booking
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# Instructions:
# 1. Setup AWS SES:
#    - Verify your domain/email in AWS SES console
#    - Move out of sandbox mode for production
# 2. Create IAM user with SES permissions:
#    - Policy: AmazonSESFullAccess (or custom SES send policy)
# 3. Install PostgreSQL and create database
# 4. Copy this file to .env and fill in your credentials
# 5. Run: python db_setup.py